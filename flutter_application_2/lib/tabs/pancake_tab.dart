import 'package:flutter/material.dart';

class PanCakeTab extends StatelessWidget {
  const PanCakeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("PanCakeTab"),
    );
  }
}
